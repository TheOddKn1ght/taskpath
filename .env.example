HOST=127.0.0.1
PORT=3000
DATABASE_PATH=./data/taskpath.sqlite
# Set your IANA timezone before first start. Defaults to your system timezone.
# TASKPATH_TIMEZONE=Europe/Moscow
# Public origin when served behind an HTTPS reverse proxy.
# TASKPATH_ORIGIN=https://tasks.example.com
# Optional session authentication (set BOTH for remote deployments).
# TASKPATH_USERNAME=me
# Generate with: bun run hash-password
# Keep the single quotes: Argon2 hashes contain dollar signs.
# TASKPATH_PASSWORD_HASH='$argon2id$v=19$m=65536,t=2,p=1$...'
# Session lifetime in whole days, from 1 to 365. Defaults to 30.
# TASKPATH_SESSION_DAYS=30
